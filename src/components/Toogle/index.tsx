import { FC } from "react";

interface IToggleProps {
    firstIconPath: string
    secondIconPath: string
}

const Toggle: FC<IToggleProps> = ({firstIconPath, secondIconPath}: IToggleProps) => {

    return (
      <div>
        
      </div>
    );
  };
  
  export default Toggle;